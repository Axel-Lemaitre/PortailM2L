extends ../node_modules/pug-bootstrap/_bootstrap

append styles
    // Bootstrap theme
    +bootswatch("paper")
    // Custom styles for this template
    link(href='../stylesheets/theme.css', rel='stylesheet')
        
block body
    // Fixed navbar
    nav.navbar.navbar-inverse
        .container
            .navbar-header
                button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
                    span.sr-only Toggle navigation
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a.navbar-brand(href='#')= "Portail M2L"
            #navbar.navbar-collapse.collapse
                ul.nav.navbar-nav
                    li
                        a(href='/') Accueil

                    li.dropdown
                        a.dropdown-toggle(href='#', data-toggle='dropdown', role='button', aria-expanded='false')
                            | Ligues
                            span.caret
                        ul.dropdown-menu(role='menu')
                            li
                                a(href='/ligues/visualiser') Visualiser
                            li
                                a(href='/ligues/ajouter') Ajouter

                    li
                        a(href='/prestations/creer') Prestations

                    li.dropdown
                        a.dropdown-toggle(href='#', data-toggle='dropdown', role='button', aria-expanded='false')
                            | Facturations
                            span.caret
                        ul.dropdown-menu(role='menu')
                            li
                                a(href='/facturations/visualiser') Visualiser
                            li
                                a(href='/facturations/etablir') Établir


                ul.nav.navbar-nav.navbar-right
                    li
                        if (!user)
                            a(href="/users/login") Login
                        else
                            a(href="/users/logout") Logout
